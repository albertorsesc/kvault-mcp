id = "broken_wikilinks"
kind = "audit"
version = "0.1.0"
protocol_version = "1.0"

module = "handler"
entrypoint = "BrokenWikilinksAudit"

provides = ["audit"]
consumes_events = []
emits_events = ["vault.audit.broken_wikilinks.completed"]

config_schema = "schema.json"
install_required = []

[tools.run]
method = "tool_run"
description = "Find [[wikilinks]] in markdown that don't resolve to any existing note."
