id = "lineage_dangling"
kind = "audit"
version = "0.1.0"
protocol_version = "1.0"

module = "handler"
entrypoint = "LineageDanglingAudit"

provides = ["audit"]
consumes_events = []
emits_events = ["vault.audit.lineage_dangling.completed"]

config_schema = "schema.json"
install_required = []

[tools.run]
method = "tool_run"
description = "Find edges in lineage.jsonl whose target doesn't appear as a frameworks.jsonl row."
