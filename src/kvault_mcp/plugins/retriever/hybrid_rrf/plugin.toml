id = "hybrid_rrf"
kind = "retriever"
version = "0.1.0"
protocol_version = "1.0"

module = "handler"
entrypoint = "HybridRrfRetriever"

provides = ["retriever"]
consumes_events = []
emits_events = []

config_schema = "schema.json"
install_required = []

[tools.search]
method = "tool_search"
description = "Hybrid FTS5 + vector search, fused via Reciprocal Rank Fusion."
